# 验收 Test Case 清单（交付前自动化验收用）

> 本文件是 `workbench-builder` skill 的**验收用例库**。每一步"生成工作台"时，
> agent 必须按本清单逐条执行，产出「验收报告」（见末尾模板），**P0 用例全部 PASS 才允许交付**。
> 六类维度与 SKILL.md「自动化验收环节」一一对应，覆盖：
> ① UI 交互逻辑 ② 样式配色 ③ 布局完整度与合理性 ④ 数据结构完整性 ⑤ 设计一致性 ⑥ 自动化任务符合预期。

执行方式：能用脚本/ grep 自检的尽量自动化；涉及渲染与交互的用浏览器/预览面板点一遍并截图留证。

### 自动化断言注意事项（Playwright 等）

- `SLOTS` 是模块内 `let` 声明、**不在 `window` 上**——`window.SLOTS['todo']` 返回 undefined，断言请用 DOM 查询（如 `document.querySelectorAll('[data-mod]')`、输入框 placeholder 等）。
- sidebar/topnav 布局是 tab 式导航，**非活跃模块 `display:none`**——基于全文长度/bodyTextLen 的断言会偏低，应按面板逐个校验。
- 在线 page URL 是文档编辑器 SPA（需登录态），不要浏览器直开；用 HTTP 200 + library API 查库代替。

---

## ① UI 交互逻辑（P0）

| TC | 用例 | 操作 | 期望 | 自检方式 |
|---|---|---|---|---|
| TC-UI-01 | 模块渲染 | 打开 HTML | 按 `WORKBENCH_CONFIG.modules` 渲染，无模块缺失/报错 | 控制台无 error；各模块标题可见 |
| TC-UI-02 | 新增记录 | 在某模块点"新增"并保存 | 记录出现，资料库 database 更新（直连模式）；本地预览降级时 localStorage 更新 | 刷新后记录仍在 |
| TC-UI-03 | 编辑记录 | 修改某字段保存 | 字段更新正确 | 导出 CSV 该字段为新值 |
| TC-UI-04 | 删除记录 | 删除一条 | 记录消失，二次确认弹窗出现 | 刷新后确已删除 |
| TC-UI-05 | 筛选/搜索 | 输入关键词 | 仅匹配项可见，其余隐藏 | 清空后恢复全量 |
| TC-UI-06 | 导出 CSV | 点"导出 CSV" | 下载文件，列名与 schema 一致 | 打开文件核对列 |
| TC-UI-07 | 导入 CSV | 导入 TC-UI-06 导出的文件 | 数据完整回填，不丢不串列 | 与导出前 diff 一致 |
| TC-UI-08 | 主题切换 | 切浅色/深色/跟随 | 配色即时变化且持久 | 刷新后仍生效 |
| TC-UI-09 | 重置 | 点"重置"并确认 | 回到默认 seed，不残留 | 控制台无报错 |
| TC-UI-10 | 风格切换 | 点工具栏「🎨 风格」按钮 → **弹窗（`.style-grid`）中点 `.style-card` 卡片** | 全局 token 切换生效（如 macaron→ocean 背景色变化）且持久 | 刷新后仍生效。⚠️ 是弹窗选卡片、**非循环切换**，勿按"点一下按钮切一档"写断言（会假 FAIL） |

## ② 样式配色（P1）

| TC | 用例 | 期望 | 自检方式 |
|---|---|---|---|
| TC-ST-01 | 设计 token 唯一来源 | 所有颜色来自 `:root` / `[data-theme]` CSS 变量，无散落硬编码色值 | `grep -Eoi '#[0-9a-f]{3,6}|rgb\(|hsl\('` 仅命中变量定义处 |
| TC-ST-02 | 深浅色对比 | 两种主题下正文/背景对比度 ≥ 4.5:1 | 目测 + 截图比对 |
| TC-ST-03 | 强调色统一 | 按钮/链接/激活态用同一 accent 变量 | 全站 grep `var(--accent)` 一致性 |
| TC-ST-04 | 无样式漂移 | 无临时 `style=` 写死颜色 | grep `style=` 检查 |

## ③ 布局完整度与合理性（P1）

| TC | 用例 | 视口 | 期望 | 自检方式 |
|---|---|---|---|---|
| TC-LO-01 | 桌面不破版 | ≥1280px | 多列/侧栏正常，无横向滚动条 | 预览面板截图 |
| TC-LO-02 | 窄屏不破版 | ≤480px | 自动单列，无重叠/溢出/错位 | 预览面板截图 |
| TC-LO-03 | 信息层级 | 任意视口 | 标题/分组/卡片层级清晰，重点突出 | 目测 |
| TC-LO-04 | 模板切换 | 改 `ui` 重开 | 4 套模板各自生效且结构正确 | 逐一切换截图 |

## ④ 数据结构完整性（P0）

| TC | 用例 | 期望 | 自检方式 |
|---|---|---|---|
| TC-DT-01 | CSV 列 = schema | 导出每个模块的 CSV | 列名与 `references/module-catalog.md` 该模块 schema 完全一致（顺序/命名） | diff 列头 |
| TC-DT-02 | seed 可落库 | 走 library `import_csv` / `create_database` | 建表成功，`database_id` 回执 | 资料库回执 |
| TC-DT-03 | 空数据安全 | 清空某模块后导出/渲染 | 不崩、有占位提示 | 手动清空测试 |
| TC-DT-04 | 超长/特殊字符 | 字段填超长文本/逗号/换行 | CSV 正确转义，不串列 | 导入导出往返 |
| TC-DT-05 | 双向同步 | 资料库改记录 → 页面刷新 | 页面反映新值（page↔database 关联正确） | 改库后重开 page |

## ⑤ 设计一致性（P1）

| TC | 用例 | 期望 | 自检方式 |
|---|---|---|---|
| TC-DS-01 | 字体统一 | 全站同一字体栈，字号按阶梯变量 | grep 字体声明 |
| TC-DS-02 | 间距/圆角统一 | 卡片/按钮间距圆角走同一组变量 | 目测 + 变量检查 |
| TC-DS-03 | 卡片风格一致 | 各模块卡片视觉语言一致 | 截图比对 |
| TC-DS-04 | 无混用视觉 | 不出现两套配色/两套圆角体系 | 目测 |

## ⑥ 自动化任务符合预期（P0，仅当第 5 步配置了才校验）

| TC | 用例 | 期望 | 自检方式 |
|---|---|---|---|
| TC-AU-01 | rrule 正确 | 每日任务 `FREQ=DAILY;...`，频率与用户要求一致 | 读 `automation_update` 回执 rrule |
| TC-AU-02 | 范围正确 | `cwds` 指向本工作台目录；`status=ACTIVE` | 读回执 |
| TC-AU-03 | prompt 自包含 | prompt 引用了正确的 `database_id` 与模块 schema，且仅追加不删改 | 读 prompt 文本 |
| TC-AU-04 | 鉴权合规 | prompt 不含任何写死 token/凭证，靠 library client 模式鉴权 | 读 prompt 文本 |
| TC-AU-05 | 模拟运行 | 手动按 prompt 跑一次（或 dry 校验） | 不报错、能产生写入计划 | 实际/模拟执行 |

---

## 验收报告模板（交付时随附）

```markdown
# 工作台验收报告 —— <工作台名> <日期>

## 概览
- 模块：<modules>
- UI 模板：<ui>  主题：<theme>
- 资料库 page：<链接>  database 节点：<database_id 列表>
- 自动化任务：<id / 无>

## 结果
| 维度 | P0 | P1 | 结论 |
|---|---|---|---|
| ① UI 交互 | ✅/❌ | — | PASS/FAIL |
| ② 样式配色 | — | ✅/❌ | PASS/FAIL |
| ③ 布局 | — | ✅/❌ | PASS/FAIL |
| ④ 数据结构 | ✅/❌ | — | PASS/FAIL |
| ⑤ 设计一致性 | — | ✅/❌ | PASS/FAIL |
| ⑥ 自动化任务 | ✅/❌ | — | PASS/FAIL（如配置）|

## 证据
- 截图：<桌面/窄屏/深浅色>
- 控制台：无 error
- 落库回执：<database_id>
- 自动化回执：<id, rrule>

## 结论
全部 P0 PASS → 允许交付 / 存在 P0 FAIL → 已退回修复（附修复项）
```
